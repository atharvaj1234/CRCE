// SciCalculator.java
interface SciCalculator {
    // Method to calculate factorial (for non-negative integers)
    long factorial(int n);

    // Method to calculate the square of a number
    double square(double num);
}